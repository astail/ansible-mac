machine:
  python:
    version: 2.7.10


dependencies:
  pre:
    - pip install ansible
  override:
    - ansible --version

test:
  override:
    - ansible-playbook -i hosts localhost.yml --syntax-check

